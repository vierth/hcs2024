# some errors will only happen when the code is run

# index errors are like htis
my_list = [1, 2, 3, 4, 5, 6]
# print(my_list[20])

# key errors are when we look for a key in a dictionary that doesn't exit
my_dict = {'run':15, 'walk':30}
# print(my_dict['amble'])

5/0